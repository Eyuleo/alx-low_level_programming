#include <stdio.h>
#include <string.h>
/**
 * _puts - prints a string followed by new line
 * @str: - points to a string type
 *
 * Return: nothing
*/
void _puts(char *str)
{
	puts(str);
	puts('\n');
}
