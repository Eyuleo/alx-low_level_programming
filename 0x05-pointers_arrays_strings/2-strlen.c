#include <string.h>
/**
 * _strlen - returns length of a string
 * @s: collection of chars or string
 *
 * Return: length of strings
*/
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
